MISTRAL_API_KEY=your-mistral-api-key-here

# Optional: override the default model (must support function/tool calling)
# AGENT_MODEL=mistral-large-latest

python verify_setup.py --repo ./target_repo 
python -m agent.main --repo ./target_repo --request "Improve the application so users can better organise and search their notes."  
cd target_repo
git diff